@model student_groupe.Models.Groupe

@{
    ViewData["Title"] = "CreateGroupe";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>CreateGroupe</h1>

<h4>Groupe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateGroupe">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Groupe_Name" class="control-label"></label>
                <input asp-for="Groupe_Name" class="form-control" />
                <span asp-validation-for="Groupe_Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="ListGroupes">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
